spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/koto
    username: koto
    password: koto123
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
